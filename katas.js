// Dado un array de numeros enteros encuentra aquel que aparece un numero de veces impar.
    // partimos de un array y queremos que se nos devuelva un parámetro

function findOdd(A) {
if (A % 2 !== 0) {
    return A;
} return none;
};

// devuelve el numero de vocales que tiene un string (a,e,i,o,u)

function getCount(s) {
    return (str.filter)
  };

const vocalCount = function(str)
// string.filter